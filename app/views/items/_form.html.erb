<%= form_for [@client, @item] do |f| %>
	<%=f.label :name %>
	<%=f.text_field :name, placeholder: "Name" %>	
	<%= f.label :price %>
	<%= f.hidden_field :client_id %>
	<%= f.text_field :price, placeholder: 'Rate' %>	
	<%=f.submit 'Save', class: 'btn btn-primary' %>
<% end %>